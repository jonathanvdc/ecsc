using static System.Console;

/// <summary>
/// The Program class, which defines the application's entry point.
/// </summary>
public static class Program
{
    /** <summary>
     * The application's entry point.
     * </summary>
     * <param name="Args">The application's argument list.</param>
     * <returns>Nothing.</returns>*/
    public static void Main(string[] Args)
    {
        WriteLine("Hello, XML docs!");
    }
}